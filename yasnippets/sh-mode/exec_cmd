# -*- mode: snippet -*-
# key: exec_cmd
# name: exec_cmd ...
# --
exec_cmd() {
    echo "$*"
    "$@"
    local status=$?
    if [ "$status" -ne 0 ]; then
        echo "Command failed with exit code $status" >&2
    fi
    return "$status"
}
